#!/usr/bin/env bash
current_dir=$(pwd)

cd ~/dev/test262-v8-machinery

if [ $1 == 'js6' ]
then
    echo 'Running tests for Spider Monkey'
    ./scripts/run-tests.sh js-dev
else
    ./scripts/run-tests.sh $@
fi

cd "$current_dir"
